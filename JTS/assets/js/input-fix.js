// Input Text Visibility Fix\n(function() {\n  'use strict';\n  \n  console.log('🎨 Loading input visibility fix...');\n  \n  function fixInputTextColor() {\n    const inputs = document.querySelectorAll('input, select, textarea');\n    \n    inputs.forEach(input => {\n      // Force dark text on light background\n      input.style.setProperty('color', '#1D1D1F', 'important');\n      input.style.setProperty('background', '#FFFFFF', 'important');\n      input.style.setProperty('-webkit-text-fill-color', '#1D1D1F', 'important');\n      \n      // Add event listeners to maintain text color\n      input.addEventListener('focus', function() {\n        this.style.setProperty('color', '#1D1D1F', 'important');\n        this.style.setProperty('background', '#FFFFFF', 'important');\n        this.style.setProperty('-webkit-text-fill-color', '#1D1D1F', 'important');\n      });\n      \n      input.addEventListener('blur', function() {\n        this.style.setProperty('color', '#1D1D1F', 'important');\n        this.style.setProperty('background', '#FFFFFF', 'important');\n        this.style.setProperty('-webkit-text-fill-color', '#1D1D1F', 'important');\n      });\n      \n      input.addEventListener('input', function() {\n        this.style.setProperty('color', '#1D1D1F', 'important');\n        this.style.setProperty('-webkit-text-fill-color', '#1D1D1F', 'important');\n      });\n    });\n  }\n  \n  // Fix inputs when DOM is ready\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', fixInputTextColor);\n  } else {\n    fixInputTextColor();\n  }\n  \n  // Also fix inputs that might be added dynamically\n  const observer = new MutationObserver(function(mutations) {\n    mutations.forEach(function(mutation) {\n      if (mutation.type === 'childList') {\n        mutation.addedNodes.forEach(function(node) {\n          if (node.nodeType === 1) { // Element node\n            const newInputs = node.querySelectorAll ? node.querySelectorAll('input, select, textarea') : [];\n            newInputs.forEach(input => {\n              input.style.setProperty('color', '#1D1D1F', 'important');\n              input.style.setProperty('background', '#FFFFFF', 'important');\n              input.style.setProperty('-webkit-text-fill-color', '#1D1D1F', 'important');\n            });\n          }\n        });\n      }\n    });\n  });\n  \n  // Start observing\n  observer.observe(document.body, {\n    childList: true,\n    subtree: true\n  });\n  \n  console.log('✅ Input visibility fix loaded');\n})();\n