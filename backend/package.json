{
  "name": "job-tracker-backend",
  "version": "2.0.0",
  "description": "Job Tracker Backend API with MongoDB Cloud Storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test": "echo 'Tests not configured yet'",
    "lint": "echo 'ESLint not configured'",
    "security:audit": "npm audit",
    "validate:env": "node -e \"require('./config/environment').validateEnvironment()\"",
    "db:status": "node -e \"require('./config/database')().then(() => console.log('DB Connected')).catch(console.error)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.6.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^7.1.3",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "job-tracker", 
    "mongodb", 
    "express", 
    "api", 
    "cloud-storage",
    "rest-api",
    "nodejs"
  ],
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "author": {
    "name": "Job Tracker Team",
    "email": "support@jobtracker.com"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/username/job-tracker.git"
  },
  "bugs": {
    "url": "https://github.com/username/job-tracker/issues"
  }
}